# Python 加法计算器 (Python Adder) 实施计划

## 任务列表

- [x] 1. **初始化项目结构**: 创建目录 `src/calculator` 和 `tests`，包含必要的 `__init__.py` 文件。
- [x] 2. **实现核心加法逻辑**: 在 `src/calculator/adder.py` 中编写 `add` 函数，并添加类型校验。
- [x] 3. **编写单元测试**: 在 `tests/test_adder.py` 中实现针对整数、浮点数及异常类型的测试用例。
- [x] 4. **运行测试并验证**: 使用 `pytest` 运行测试，确保所有验收标准均已通过。

## 执行说明
- 任务粒度已拆解为原子化操作。
- 严禁在任务勾选前进行任何非任务相关的代码修改。
